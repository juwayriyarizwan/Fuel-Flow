<!DOCTYPE html>
<html lang="en">
<meta charset="UTF-8">
  <style>
h1 {
    text-align: center;
    margin-top: 40px;
    margin-bottom: 60px;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
  }
  #userinfo{
  width: 300px;
  height: 30px;
  }
  .login{
  position: absolute;
  top: 30%;
  left: 50%;
  transform: translate(-50%, -50%);
  height: 500px;
  overflow-y: auto;
  }

  .table-container {
    height: 500px;
    overflow: auto;
  }

  table {
    border-collapse: collapse;
    border-spacing: 0;
    width: 100%;
  }

  th, td {
    border: 1px solid #c1c1c1;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
    font-size: 14px;
    padding: 10px 5px;
    text-align: left;
    vertical-align: top;
    word-break: normal;
  }

  th {
    background-color: #f1f1f1;
  }

  button {
    background-color: #007aff;
    border: none;
    border-radius: 4px;
    color: white;
    cursor: pointer;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
  }
  </style>
  <h1>Fuel Quote History</h1>
  <form action="/quotehist" method="POST">
  <div class="login">
  <style type="text/css">
.tg  {border-collapse:collapse;border-spacing:0;}
.tg td{border-color:black;border-style:solid;border-width:1px;font-family:Arial, sans-serif;font-size:14px;
  overflow:hidden;padding:10px 5px;word-break:normal;}
.tg th{border-color:black;border-style:solid;border-width:1px;font-family:Arial, sans-serif;font-size:14px;
  font-weight:normal;overflow:hidden;padding:10px 5px;word-break:normal;}
.tg .tg-0pky{border-color:inherit;text-align:left;vertical-align:top}
</style>
<div class ="table-container">
<table>
<thead>
  <tr>
    <th>Gallons Requested</th>
    <th>Delivery Address</th>
    <th>Delivery Date</th>
    <th>Suggested Price</th>
    <th>Total Amount Due</th>
  </tr>
</thead>
<tbody>
  <% for(let i=0; i<QuoteHist.length;i++) {%>
  <tr>
    <td><%= QuoteHist[i].gallonreq %></td>
    <td><%= QuoteHist[i].deliveryaddress%></td>
    <td><%= QuoteHist[i].deliverydate%></td>
    <td><%= QuoteHist[i].suggestedpricepergallon %></td>
    <td><%= QuoteHist[i].totalamountdue%></td>
  </tr>
  <% } %>
</tbody>
</table>
</div>
  </div>
</form>
  </body>
  <div>
  <button onclick="location.href='/quoteform'"+>Check User Quote Form</button>
  <form action="/logout" method="POST">
    <button type="submit">Log Out</button>
  </form>
</div>
<form action="/index" method="POST">
	<button type="submit">Main Page</button>
  </form>
</html>
